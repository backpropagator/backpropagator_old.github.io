#include <bits/stdc++.h>

#define forn(i, n) for (int i = 0; i < int(n); i++)

typedef long long li;

using namespace std;

int main(){
	int w;
	cin>>w;
	if(w%2 == 0 && w != 2)
		cout<<"YES\n";
	else
		cout<<"NO\n";
}