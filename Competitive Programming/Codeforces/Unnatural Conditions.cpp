#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main(){
	ll n, m;
	cin>>n>>m;
	int len = 400;
	cout << string(len, '5') << "\n";
  	cout << (string(len - 1, '4') + '5') << "\n";
}